package ${package.Service};

import ${superServiceClassPackage};

/**
 * ${table.comment!} 数据操作服务接口。
 *
 * @author ${author}
 * @since ${date}
 */
<% if(kotlin){ %>
interface ${table.serviceName} : ${superServiceClass}<${entity}>
<% }else{ %>
public interface ${table.serviceName} extends ${superServiceClass}<${entity}, ${voConfig.className}> {

}
<% } %>