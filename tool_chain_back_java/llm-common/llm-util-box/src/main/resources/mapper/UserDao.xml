<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ctdi.cnos.llm.system.user.dao.UserDao">

    <select id="countPlatformUser" resultType="long">
        select count(1) from meta.mm_user where last_active_time is not null and (tool_auth='0' or agent_auth='0')
    </select>

    <select id="countCompany" resultType="long">
        select count(distinct corp_name) from meta.mm_user where last_active_time is not null and corp_name is not null and (tool_auth='0' or agent_auth='0')
    </select>


    <update id="updateBatchAuthByEmployeeNumbers" parameterType="java.util.List" >
        <foreach collection="list" item="item" index="index" separator=";" >
            update meta.mm_user
            <set >
                <if test="item.agentAuth != null and item.agentAuth != ''">agent_auth = #{item.agentAuth},</if>
                <if test="item.toolAuth != null and item.toolAuth != ''">tool_auth = #{item.toolAuth},</if>
                <if test="item.modifyDate != null">modify_date = #{item.modifyDate},</if>
                <if test="item.modifierId != null and item.modifierId != ''">modifier_id = #{item.modifierId},</if>
            </set>
            where employee_number = #{item.employeeNumber}
        </foreach>
    </update>

</mapper>